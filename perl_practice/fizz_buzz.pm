#!/usr/bin/perl
use warnings;
use strict;
use Data::Dumper;


my $total;
my $fizzpoint;
sub fizzbuzz{
    $fizzpoint = shift;
    for (my $i = 0; $i <= $fizzpoint) {
        if ($i/3 && /\\D/){
        
        }
    }
}
